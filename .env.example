# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Chrome/Chromium Path for Local Development
# Only needed if Chrome/Edge is not in standard location
# CHROME_PATH=/path/to/chrome

# Vercel Environment (automatically set in production)
# VERCEL=1

# AWS Lambda Environment (automatically set if using Lambda)
# AWS_LAMBDA_FUNCTION_NAME=function-name
